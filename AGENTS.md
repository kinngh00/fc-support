# FC-SUPPORT 작업 강제 규칙

이 파일의 규칙은 이 저장소에서 작업하는 모든 Codex 에이전트에게 의무적으로 적용된다. 사용자가 매번 금지사항을 반복하거나 결과를 확인해야 할 책임은 없다. 요청 범위를 지키는 책임은 전적으로 에이전트에게 있다.

## 1. 명시된 요청만 변경

- 시스템·보안상 허용되는 범위에서 사용자의 문장을 최우선 실행 기준으로 삼고, 표현을 임의로 완화하거나 다른 의미로 바꾸지 않는다.
- 사용자 메시지의 문장, 조건, 예외, 강조 표현을 각각 독립된 원자 요구사항으로 분해하며 하나도 합치거나 누락하지 않는다.
- "받아오지 마"는 받아온 뒤 버리는 것이 아니라 해당 네트워크 요청 자체를 하지 않는다는 뜻으로 처리한다.
- "없애"는 단순히 시각적으로 가리는 것이 아니라 사용자가 지정한 범위에서 요소·문구·동작이 존재하지 않게 한다. 사용자가 표시만 숨기라고 한 경우에만 CSS 숨김을 허용한다.
- "그대로 유지"와 언급되지 않은 영역은 변경 불가 조건으로 취급한다.
- 사용자가 이번 요청에서 명시한 동작, 요소, 문구만 수정한다.
- 요청에서 언급하지 않은 기존 UI의 위치, 크기, 색상, 간격, 구조, 데이터 출처, 동작은 변경하지 않는다.
- 더 좋아 보인다는 판단, 코드 정리, 일관성 개선, 발견한 다른 문제를 이유로 범위를 넓히지 않는다.
- 요청을 구현하기 위해 언급되지 않은 동작을 바꿔야 한다면 작업을 중단하고 변경 전에 사용자에게 질문한다.
- 이전 대화의 요구사항은 현재 요청과 충돌하지 않는 한 그대로 보존한다.
- 더미 데이터, 추정 데이터, 임의 값으로 실제 데이터의 빈자리를 채우지 않는다.
- 에이전트의 취향, 관행, 일관성, 편의, 최적화 판단은 변경 권한이 아니다.
- 요구사항이 모호하거나 서로 충돌하면 임의로 선택하지 말고 쓰기 전에 사용자에게 질문한다.

## 2. 쓰기 전 변경 범위 선언

- 파일을 수정하기 전에 `.codex/change-scope.local.json`을 생성한다.
- `task`에는 현재 사용자 요청을 그대로 요약한다.
- `userInstructions`에는 사용자 메시지의 지시 문장을 빠짐없이 가능한 한 원문 그대로 배열로 기록한다.
- `requirements`에는 각 지시를 `R1`, `R2` 형식의 고유 ID로 하나씩 분리하고 `instruction`, `implementation`, `status`, `evidence`를 기록한다.
- 여러 지시를 하나의 requirement로 합치는 것을 금지한다. 접속사로 연결된 독립 조건도 각각 분리한다.
- 작업 시작 시 모든 requirement의 `status`는 `pending`이어야 한다. 실제 검증이 끝난 항목만 `passed`로 변경한다.
- `assumptions`는 반드시 빈 배열이어야 한다. 가정이 필요하면 작업하지 말고 사용자에게 질문한다.
- `allowedFiles`에는 수정이 허용된 파일을 저장소 기준 정확한 경로로 하나씩 적는다. 와일드카드와 디렉터리 전체 허용은 금지한다.
- `allowedRegions`에는 각 파일에서 변경할 함수, 컴포넌트, CSS 선택자 또는 설정 항목을 구체적으로 적는다.
- `changeMap`에는 변경 파일의 각 영역과 이를 허용한 requirement ID를 연결한다. 요구사항 ID와 연결되지 않은 변경 영역은 금지한다.
- UI 변경이면 `visualChange`를 `true`로 설정하고, `visualVerification.status`는 검증 전 `pending`, 검증 완료 후에만 `passed`로 바꾼다.
- 범위 선언에 없는 파일 또는 영역이 필요해지면 먼저 사용자에게 이유와 영향을 알리고 승인을 받은 뒤 범위를 갱신한다.

## 3. 수정 중 금지사항

- 범위 밖 파일이나 영역을 수정하지 않는다.
- 요청과 무관한 리팩터링, 이름 변경, 정렬, 포맷 변경, 의존성 변경을 함께 수행하지 않는다.
- 기존 변경사항을 임의로 되돌리거나 덮어쓰지 않는다.
- 문제를 발견했지만 요청 범위가 아니면 수정하지 않고 보고만 한다.
- 사용자의 명시적 승인 없이 새로운 디자인 결정을 만들지 않는다.
- 요청을 구현했다는 이유로 주변 코드나 같은 컴포넌트의 다른 문제를 함께 수정하지 않는다.
- 사용자 지시보다 범위를 좁혀 일부만 구현하거나, 더 넓혀 추가 구현하는 것을 모두 금지한다.
- 실제 API가 제공하지 않는 값을 유추하거나 다른 출처의 값으로 대체하지 않는다.

## 4. 검증 및 배포 차단

- 수정 후 `npm run verify:scope`와 `git diff --check`를 반드시 통과한다.
- 모든 diff 줄을 사용자 요청과 일대일로 대조한다. 대응되는 요청이 없는 변경은 제거한다.
- 모든 requirement가 `passed`이고 각각 하나 이상의 구체적인 검증 근거가 있어야만 커밋·푸시·배포할 수 있다.
- `assumptions`가 하나라도 있으면 커밋·푸시·배포를 금지한다.
- 변경된 모든 파일은 `changeMap`에서 하나 이상의 requirement ID와 연결되어야 한다.
- UI 변경은 사용자가 지정한 브라우저에서 대상 화면을 실제로 열고 데스크톱과 모바일을 확인한다.
- UI 검증 도구를 사용할 수 없거나 캡처를 확인하지 못했다면 `visualVerification.status`와 관련 requirement를 `passed`로 바꾸지 않는다.
- 현재 개발 단계에서는 모든 요구사항 검증이 끝난 변경을 같은 작업 안에서 반드시 `localhost:3000`에 반영한다. 검증을 생략한 채 반영하거나, 검증을 끝내고도 반영을 누락하는 것을 모두 금지한다.
- 테스트 실패 또는 범위 검사 실패 상태에서는 커밋·푸시·운영 배포를 금지한다.
- 커밋 훅이나 배포 검사를 우회하는 옵션(`--no-verify` 등)은 사용하지 않는다.

## 5. 완료 보고

- 실제로 변경한 항목만 보고한다.
- 확인하지 못한 내용을 확인했다고 말하지 않는다.
- requirement가 하나라도 `pending`이면 완료, 수정 완료, 반영 완료라고 표현하지 않는다.
- 완료 보고에는 각 requirement가 어떻게 구현되고 무엇으로 검증됐는지 빠짐없이 대응시킨다.
- 변경한 파일과 영역이 범위 선언과 일치하는지 마지막으로 확인한다.
- 작업 완료 후 `.codex/change-scope.local.json`을 삭제한다.
