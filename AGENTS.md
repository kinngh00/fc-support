# FC-SUPPORT 작업 강제 규칙

이 파일의 규칙은 이 저장소에서 작업하는 모든 Codex 에이전트에게 의무적으로 적용된다. 사용자가 매번 금지사항을 반복하거나 결과를 확인해야 할 책임은 없다. 요청 범위를 지키는 책임은 전적으로 에이전트에게 있다.

## 1. 명시된 요청만 변경

- 사용자가 이번 요청에서 명시한 동작, 요소, 문구만 수정한다.
- 요청에서 언급하지 않은 기존 UI의 위치, 크기, 색상, 간격, 구조, 데이터 출처, 동작은 변경하지 않는다.
- 더 좋아 보인다는 판단, 코드 정리, 일관성 개선, 발견한 다른 문제를 이유로 범위를 넓히지 않는다.
- 요청을 구현하기 위해 언급되지 않은 동작을 바꿔야 한다면 작업을 중단하고 변경 전에 사용자에게 질문한다.
- 이전 대화의 요구사항은 현재 요청과 충돌하지 않는 한 그대로 보존한다.
- 더미 데이터, 추정 데이터, 임의 값으로 실제 데이터의 빈자리를 채우지 않는다.

## 2. 쓰기 전 변경 범위 선언

- 파일을 수정하기 전에 `.codex/change-scope.local.json`을 생성한다.
- `task`에는 현재 사용자 요청을 그대로 요약한다.
- `allowedFiles`에는 수정이 허용된 파일을 저장소 기준 정확한 경로로 하나씩 적는다. 와일드카드와 디렉터리 전체 허용은 금지한다.
- `allowedRegions`에는 각 파일에서 변경할 함수, 컴포넌트, CSS 선택자 또는 설정 항목을 구체적으로 적는다.
- UI 변경이면 `visualChange`를 `true`로 설정하고, `visualVerification.status`는 검증 전 `pending`, 검증 완료 후에만 `passed`로 바꾼다.
- 범위 선언에 없는 파일 또는 영역이 필요해지면 먼저 사용자에게 이유와 영향을 알리고 승인을 받은 뒤 범위를 갱신한다.

## 3. 수정 중 금지사항

- 범위 밖 파일이나 영역을 수정하지 않는다.
- 요청과 무관한 리팩터링, 이름 변경, 정렬, 포맷 변경, 의존성 변경을 함께 수행하지 않는다.
- 기존 변경사항을 임의로 되돌리거나 덮어쓰지 않는다.
- 문제를 발견했지만 요청 범위가 아니면 수정하지 않고 보고만 한다.
- 사용자의 명시적 승인 없이 새로운 디자인 결정을 만들지 않는다.

## 4. 검증 및 배포 차단

- 수정 후 `npm run verify:scope`와 `git diff --check`를 반드시 통과한다.
- 모든 diff 줄을 사용자 요청과 일대일로 대조한다. 대응되는 요청이 없는 변경은 제거한다.
- UI 변경은 사용자가 지정한 브라우저에서 대상 화면을 실제로 열고 데스크톱과 모바일을 확인한다.
- UI 검증 도구를 사용할 수 없거나 캡처를 확인하지 못했다면 `visualVerification.status`를 `passed`로 바꾸지 않고 완료 보고에 검증 미완료 사실을 정확히 적는다.
- 현재 개발 단계에서는 사용자가 요청한 변경을 범위 검사와 테스트 통과 후 항상 `localhost:3000`에 즉시 반영한다. 시각 검증 미완료만을 이유로 반영을 보류하지 않는다.
- 테스트 실패 또는 범위 검사 실패 상태에서는 커밋·푸시·운영 배포를 금지한다.
- 커밋 훅이나 배포 검사를 우회하는 옵션(`--no-verify` 등)은 사용하지 않는다.

## 5. 완료 보고

- 실제로 변경한 항목만 보고한다.
- 확인하지 못한 내용을 확인했다고 말하지 않는다.
- 변경한 파일과 영역이 범위 선언과 일치하는지 마지막으로 확인한다.
- 작업 완료 후 `.codex/change-scope.local.json`을 삭제한다.
